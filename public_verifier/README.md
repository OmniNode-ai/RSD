# omninode-grant-verifier

The public, offline verifier for the signed executable-grant contract used by
`omninode-rsd`. It validates the signed contract and its bundled public
resources without contacting a service or enabling delegation execution.

This distribution is published separately from `omninode-rsd`. Use matching
reviewed versions of both distributions when installing the RSD package.
